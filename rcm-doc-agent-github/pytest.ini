[pytest]
; `src` isn't an installed package -- add the repo root to sys.path so
; `from src.docproc... import ...` works the same way it does in scripts/
; and the CLI entrypoints (which do `sys.path.insert(0, repo_root)` by hand).
pythonpath = .
testpaths = tests
